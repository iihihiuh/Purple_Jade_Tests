MOVS r7, 128
MOVS r1, 8
LSLS r7, r7, r1
MOVS r1, 0
ORRS r1, r7
MOVS r7, 0
MOVS r3, 8
LSLS r7, r7, r3
MOVS r3, 2
ORRS r3, r7
DIVU r1, r1, r3
DIVU r1, r1, r3
DIVU r1, r1, r3
DIVU r1, r1, r3
MULU r2, r2, r1
MULU r2, r2, r1
MULU r2, r2, r1
MULU r2, r2, r1
ADDS r2, r1, 0
ADDS r2, r1, 0
ADDS r2, r1, 0
ADDS r2, r1, 0
ADDS r2, r1, 0
ADDS r2, r1, 0
ADDS r2, r1, 0
ADDS r2, r1, 0
ANDS r2, r1
ANDS r2, r1
ANDS r2, r1
ANDS r2, r1
ANDS r2, r1
ANDS r2, r1
ANDS r2, r1
ANDS r2, r1
LDR r2, [r1, 4]
LDR r2, [r1, 4]
LDR r2, [r1, 4]
LDR r2, [r1, 4]
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
DIVU r1, r1, r3
DIVU r1, r1, r3
DIVU r1, r1, r3
DIVU r1, r1, r3
MULU r1, r1, r3
MULU r1, r1, r3
MULU r1, r1, r3
MULU r1, r1, r3
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ANDS r1, r1
ANDS r1, r1
ANDS r1, r1
ANDS r1, r1
ANDS r1, r1
ANDS r1, r1
ANDS r1, r1
ANDS r1, r1
LDR r1, [r1, 4]
LDR r1, [r1, 4]
LDR r1, [r1, 4]
LDR r1, [r1, 4]
LSLS r1, r1, r2
LSLS r1, r1, r2
LSLS r1, r1, r2
LSLS r1, r1, r2
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
ADDS r1, r1, 0
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
MOVS r7, 0
MOVS r2, 8
LSLS r7, r7, r2
MOVS r2, 0
ORRS r2, r7
DIVU r1, r1, r3
DIVU r1, r1, r3
DIVU r1, r1, r3
DIVU r1, r1, r3
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
STR r1, [r2, 4]
LDR r3, [r2, 4]
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
NOOP
MOVS r7, 0
MOVS r1, 8
LSLS r7, r7, r1
MOVS r1, 0
ORRS r1, r7
ADDS r1, r1, 0
BEQ NOOP0
NOOP
BEQ NOOP1
NOOP
BEQ NOOP2
NOOP
BEQ NOOP3
NOOP
ADDS r1, r1, 0
NOOP0: NOOP
NOOP1: NOOP
NOOP2: NOOP
NOOP3: NOOP
MOVS r7, 0
MOVS r1, 8
LSLS r7, r7, r1
MOVS r1, 0
ORRS r1, r7
ADDS r1, r1, 0
BNE NOOP4
NOOP
BEQ NOOP5
NOOP
BEQ NOOP6
NOOP
BEQ NOOP7
NOOP
ADDS r1, r1, 0
NOOP4: NOOP
NOOP5: NOOP
NOOP6: NOOP
NOOP7: NOOP
MOVS r7, 0
MOVS r1, 8
LSLS r7, r7, r1
MOVS r1, 0
ORRS r1, r7
ADDS r1, r1, 0
BNE NOOP8
NOOP
ADDS r4, r6, 0
BNE NOOP9
NOOP
ADDS r3, r6, 0
BEQ NOOP10
NOOP
BEQ NOOP11
NOOP
ADDS r1, r1, 0
NOOP8: NOOP
NOOP9: NOOP
NOOP10: NOOP
NOOP11: NOOP
